// import Navbar from '@/components/navbar/Navbar';

export default function typeLayout({
    children
}) {
    return (
        <div>
            {/* <Navbar /> */}
            {children}
        </div>
    );
}